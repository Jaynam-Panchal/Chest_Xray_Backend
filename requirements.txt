flask==2.3.4
flask-cors==4.0.0
# PyTorch - Using stable version compatible with Python 3.11
# For CPU-only (recommended for Render to avoid large builds)
--index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchvision==0.20.1+cpu
timm==1.0.12
pillow==11.0.0
groq==0.3.0
python-dotenv==1.0.0
gunicorn==23.0.0
huggingface-hub==0.26.5